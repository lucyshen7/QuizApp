// Helper functions

const indexToLetter = function(index) { // Takes an index and returns the corresponding letter
  const map = ['A', 'B', 'C', 'D', 'E', 'F', 'G'];
  return map[index];
};

module.exports = { indexToLetter };
